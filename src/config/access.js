// access rules (public pages have now guard property in the route Obj)
const accessRules = {
    authenticatedUser: 'authenticated user',
    authenticatedSpecificUserOrAdmin: 'authenticated specific user or admin user'
};

export default accessRules;
